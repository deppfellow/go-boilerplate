// Package server defines the core `Server` struct that brings
// all dependencies together (Config, Logger, Database, Redis).
//
// It contains the initialization logic to spin up the HTTP server
// and handles graceful shutdowns
package server
